package src;

import java.util.List;

public class Routing {
  public static void dijkstra_naive(List<List<Vertex>> adjList, int s, int t) {
    // int vertexCount = adjList.size();
    // int[] bw = new int[vertexCount];
    // int[] status = new int[vertexCount];
    // List<Vertex> dad = new 
    // List<Vertex> startList = adjList.get(s);
    // for (int i = 0; i < vertexCount; i++) {
    //   status[i] = 0;
    // }
    // for (int i = 0; i < startList.size(); i++) {
    //   Vertex v = startList.get(i);
    //   status[v.getName()] = 1;
    // }
  }

  public static void dijkstra_heap(List<List<Vertex>> adjList, int s, int t) {

  }

  public static void kruskal(List<List<Vertex>> adjList, int s, int t) {

  }
}
